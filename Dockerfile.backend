FROM node:22-alpine AS production

ENV NODE_ENV=production
WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev --ignore-scripts && npm cache clean --force

COPY server.js ./
COPY login.html signup.html dashboard.html ./

USER node
EXPOSE 3000

CMD ["node", "server.js"]
